@model ReviewMe.ViewModel.ReviewMapViewModel

@{
    Layout = null;
}
@*Test*@
@*<link href="~/Content/prism.css" rel="stylesheet" />
<link href="~/Content/chosen.css" rel="stylesheet" />
<script src="~/Scripts/chosen.jquery.js"></script>
<script src="~/Scripts/prism.js"></script>

<style type="text/css">
    <!-- this will set our autocomplete width -->
    #jacComplete {
        width: 400px;
        display: block;
    }
</style>*@

<script type="text/javascript">
     @*Test*@
    //var config = {
    //    '.chosen-select-width': { width: "75%" }
    //}
    //for (var selector in config) {
    //    $(selector).chosen(config[selector]);
    //}

    $(document).ready(function () {

         @*Test*@
        //$("#test").change(function () {
        //    var a = $("#test :selected").text();
        //    $("#selectedlist").html(a);
        //});

        //$("#s-all").click(function () {

        //    var a = $("#test option");

        //    $.each(a, function (i, val) {
        //        $(val).change();
        //    });

        //    var b = $("#test option").text();
        //    $("#selectedlist").html(b);

        //});

        //$("#c-all").click(function () {

        //    $("#test option:selected").removeAttr("selected");

        //    var a = $("#test :selected").text();
        //    $("#selectedlist").html(a);
        //    $(".chosen-choices").find("li.search-choice").remove();

        //});

        $("#ReviewerId").change(function () {
            debugger;
            //$("#RevieweeList").empty();
            //$("#RevieweeList").html("<option value disable></option>");
            //$(this).find('option').each(function () {
            //    debugger;
            //    if ($(this).attr("value") != "")
            //    {
            //        $("#RevieweeList").html("<option value="+$(this).attr("value")+">"+$(this).text()+"</option>");
            //    }
            //});
            var ReviewerId = $(this).val();
            if (ReviewerId == "")
            {
                $('#RevieweeId').empty();
                return;
            }

            //$("#RevieweeList").find('option').each(function () {
            //    debugger;
            //    if ($(this).val() == ReviewerId)
            //    {
            //        debugger;
            //        $(this).remove();
            //    }
            //});
            $.ajax({
                url: "/ReviewMap/GetRevieweeList/" + ReviewerId,
                type: "GET",
                success: function (data) {
                    debugger;
                    $('#RevieweeId').empty();
                    $('#RevieweeId').append(data);
                },
                error: function (response) {
                    debugger;
                }
            });

        });
    });
</script>

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h4 class="modal-title">Create Group</h4>
</div>
<div class="modal-body">
    @using (Html.BeginForm("AddEditGroup", "ReviewMap", FormMethod.Post, new { @class = "form-horizontal", enctype = "multipart/form-data" }))
    {
        <div class="row">
            <div class="col-sm-9">

                <div>
                    <label class="control-label">Reviewer<span style="color: red;">*</span></label>
                </div>
                @Html.DropDownListFor(m => m.ReviewerId, new SelectList(Model.DropDownForReviewer, "Value", "Text"), "Select Reviewer", new { @class = "form-control" })
                @Html.ValidationMessageFor(m => m.ReviewerId)

                <div>
                    <label class="control-label">Reviewee<span style="color: red;">*</span></label>
                </div>

                <div id="RevieweeId">
                </div>

                <br />
                <span class="RevieweeErrorMessage" style="color: red;">
                </span>
            </div>
        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            <button type="submit" class="btn btn-primary" id="btnSave">Save changes</button>
        </div>
        @Html.Hidden("SelectedListValues")
    }
</div>

 @*Test*@
@*<script type="text/javascript">

    var config = {
        '.chosen-select': {},
        '.chosen-select-deselect': { allow_single_deselect: true },
        '.chosen-select-no-single': { disable_search_threshold: 10 },
        '.chosen-select-no-results': { no_results_text: 'oops, nothing found!' },
        '.chosen-select-width': { width: "95%" }
    }
    for (var selector in config) {
        $(selector).chosen(config[selector]);
    }
</script>*@
<script type="text/javascript">
    var selectList = "";
    $("#btnSave").click(function () {
        //debugger;
        var revieweeList = "";
        $('li.search-choice').each(function () {
            //debugger;
            //revieweeList += $(this).find('a.search-choice-close').data("option-array-index") + ",";
            revieweeList += $(this).find("span").attr("id") + ",";
        });
        revieweeList = revieweeList.substring(0, revieweeList.length - 1);
        $('#SelectedListValues').val(revieweeList);
        debugger;
        if (revieweeList.length == 0) {
            $('.RevieweeErrorMessage').html("Please select alteast one Reviewee.");
            return false;
        }
    });

</script>